name: Security Scan

on:
  push:
    branches: [main, master, dev]
  pull_request:
    branches: [main, master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write

jobs:
  grype-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        host: [honor, pro]

    steps:
      - uses: actions/checkout@v7

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            cores = 0
            max-jobs = auto

      - name: Cache Nix store
        uses: actions/cache@v6
        with:
          persist-credentials: false

      - name: Cache Grype database
        uses: actions/cache@v6
        with:
          path: ~/.cache/grype/db
          key: grype-db-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            grype-db-${{ runner.os }}-

      - name: Build system closure
        id: build
        run: |
          nix build .#nixosConfigurations.${{ matrix.host }}.config.system.build.toplevel --print-out-paths > toplevel-path.txt
          echo "toplevel=$(cat toplevel-path.txt)" >> $GITHUB_OUTPUT

      - name: Generate SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0
        with:
          path: ${{ steps.build.outputs.toplevel }}
          format: spdx-json
          output-file: sbom.spdx.json

      - name: Run Grype scan
        uses: anchore/scan-action@1638637db639e0ade3258b51db49a9a137574c3e # v6
        id: grype
        with:
          sbom: sbom.spdx.json
          severity-cutoff: medium
          fail-build: true

      - name: Upload SARIF results
        uses: github/codeql-action/upload-sarif@54f647b7e1bb85c95cddabcd46b0c578ec92bc1a # v4
        if: always()
        with:
          sarif_file: results.sarif
          category: grype-${{ matrix.host }}

      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: security-reports-${{ matrix.host }}
          path: |
            sbom.spdx.json
            results.sarif
          retention-days: 30